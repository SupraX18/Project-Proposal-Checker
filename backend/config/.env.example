PORT=43121
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/proposal_checker
JWT_SECRET=replace_this_with_a_long_random_secret
DOCUMENT_STORAGE=database
# Optional when the frontend is served by the same backend URL.
# CLIENT_ORIGIN=http://localhost:5174

# Email Configuration (Required for sending OTPs instead of console logging)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM="Project Proposal Checker" <your-email@gmail.com>
